# Issue tracker (spec repo)

This **is** the spec repository for the application.

- **SPEC_REPO:** set in target repos’ `docs/agents/issue-tracker.md` to this repo’s `owner/name`.
- **CLI:** `gh issue create`, `gh issue view`, `gh issue list`

Child implementation repos link **parent** issues here.
